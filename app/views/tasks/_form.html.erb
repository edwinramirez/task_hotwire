<%= form_with(model: task, class: "mb-7") do |form| %>
  <div class="mb-5">
    <%= form.text_field :description, placeholder: "Add new task" %>
    <%= form.submit "Add", class: "btn py-2 ml-2 bg-blue-600 text-white" %>
  </div>
<% end %>