<div>
  <h1 class="font-bold text-2xl mb-3">Edit Task</h1>

  <turbo-frame id="<%= dom_id @task %>">
    <%= render "form", task: @task %>
    <%= link_to "Cancel", tasks_path, class: "btn mb-3 bg-gray-100" %>
  </turbo-frame>
</div>